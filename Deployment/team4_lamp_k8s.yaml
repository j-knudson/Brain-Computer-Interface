# Save the output of this file and use kubectl create -f to import
# it into Kubernetes.
#
# Created with podman-3.4.4
apiVersion: v1
kind: Pod
metadata:
  creationTimestamp: "2023-02-12T16:44:15Z"
  labels:
    app: pytenjpod
  name: 475Team4-pod
spec:
  containers:
    - args:
        - start-notebook.sh
      image: localhost/pyspark_tensor_jupyter_475:latest
      name: pytenjup
      ports:
        - containerPort: 8888
          hostPort: 10000
      resources: {}
      securityContext:
        capabilities:
          drop:
            - CAP_MKNOD
            - CAP_NET_RAW
            - CAP_AUDIT_WRITE
      volumeMounts:
        - mountPath: /home/jovyan/work
          name: BCI_Mounted
    - args:
        - nginx
        - -g
        - daemon off;
      image: docker.io/library/nginx:latest
      name: ngix
      ports:
        - containerPort: 80
          hostPort: 9000
      resources: {}
      securityContext:
        capabilities:
          drop:
            - CAP_MKNOD
            - CAP_NET_RAW
            - CAP_AUDIT_WRITE
  restartPolicy: Always
  volumes:
    - hostPath:
        path: ./BCI_Mounted
        type: Directory
      name: BCI_Mounted
status: {}
